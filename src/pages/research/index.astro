---
import { getCollection } from "astro:content";
import BaseLayout from "@/layouts/BaseLayout.astro";
import PaperCard from "@/components/PaperCard.astro";
import TalkEntry from "@/components/TalkEntry.astro";

const allPapers = await getCollection("papers", ({ data }) => !data.draft);
const papersByYear = allPapers.sort((a, b) => b.data.year - a.data.year || (b.data.month ?? 0) - (a.data.month ?? 0));
const years = [...new Set(papersByYear.map((p) => p.data.year))];

const talks = (await getCollection("talks", ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const researchCode = (await getCollection("research-code", ({ data }) => !data.draft))
  .sort((a, b) => a.data.order - b.data.order);
---
<BaseLayout title="Research Projects — Yuanjia (Scott) Yang">
  <h1 class="text-3xl font-bold tracking-tight">Research Projects</h1>

  <!-- Research Projects — visual cards -->
  <section class="mt-10">
    <h2 class="text-xs font-semibold uppercase tracking-widest text-zinc-400 dark:text-zinc-500">Projects</h2>
    <div class="mt-4 grid gap-4 sm:grid-cols-2">
      {researchCode.map((rc) => {
        const hasDetail = rc.body && rc.body.trim();
        const href = hasDetail ? `/research/${rc.id}` : rc.data.repo;
        const isExternal = !hasDetail;
        return (
          <a
            href={href}
            target={isExternal ? "_blank" : undefined}
            rel={isExternal ? "noopener noreferrer" : undefined}
            class="group block overflow-hidden rounded-lg border border-zinc-200 transition-all hover:border-accent hover:shadow-lg dark:border-zinc-800 dark:hover:border-accent"
          >
            <div class="aspect-[16/9] bg-gradient-to-br from-accent/20 via-zinc-100 to-zinc-50 dark:from-accent/10 dark:via-zinc-800 dark:to-zinc-900 flex items-center justify-center">
              <span class="text-2xl font-bold text-accent/40 dark:text-accent/30">{rc.data.title}</span>
            </div>
            <div class="p-4">
              <div class="flex items-start justify-between">
                <p class="text-sm font-semibold text-zinc-900 group-hover:text-accent dark:text-zinc-100">{rc.data.title}</p>
                {rc.data.demo && (
                  <span class="shrink-0 ml-2 rounded-full bg-accent/10 px-2 py-0.5 text-[10px] font-medium text-accent">Live demo</span>
                )}
              </div>
              <p class="mt-1 text-xs text-zinc-500 dark:text-zinc-400 line-clamp-2">{rc.data.blurb}</p>
              <div class="mt-3 flex flex-wrap gap-1">
                {rc.data.tags.slice(0, 4).map((tag: string) => (
                  <span class="rounded-full bg-zinc-100 px-2 py-0.5 text-[10px] text-zinc-500 dark:bg-zinc-800 dark:text-zinc-400">{tag}</span>
                ))}
              </div>
              <p class="mt-2 text-[10px] text-zinc-400 dark:text-zinc-500">{rc.data.org}</p>
            </div>
          </a>
        );
      })}
    </div>
  </section>

  <!-- Publications -->
  <section class="mt-14">
    <h2 class="text-xs font-semibold uppercase tracking-widest text-zinc-400 dark:text-zinc-500">Publications</h2>
    {years.map((year) => (
      <div class="mt-6">
        <h3 class="text-sm font-medium text-zinc-300 dark:text-zinc-600">{year}</h3>
        {papersByYear.filter((p) => p.data.year === year).map((paper) => (
          <PaperCard
            title={paper.data.title}
            authors={paper.data.authors}
            venue={paper.data.venue}
            year={paper.data.year}
            status={paper.data.status}
            pdf={paper.data.pdf}
            code={paper.data.code}
            demo={paper.data.demo}
            arxiv={paper.data.arxiv}
          />
        ))}
      </div>
    ))}
  </section>

  <!-- Talks -->
  <section class="mt-14">
    <h2 class="text-xs font-semibold uppercase tracking-widest text-zinc-400 dark:text-zinc-500">Talks & Posters</h2>
    <div class="mt-4">
      {talks.map((talk) => (
        <TalkEntry
          title={talk.data.title}
          venue={talk.data.venue}
          date={talk.data.date}
          kind={talk.data.kind}
          poster={talk.data.poster}
          slides={talk.data.slides}
        />
      ))}
    </div>
  </section>
</BaseLayout>
